server.port = 9998
#hostname = "test.com"
#server.address = 109.228.2.80

spring.datasource.url=jdbc:mysql://localhost/toms_oauth2?zeroDateTimeBehavior=convertToNull
spring.datasource.username=root
spring.datasource.password=silanis1
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.hibernate.naming_strategy=org.hibernate.cfg.EJB3NamingStrategy

# spring.datasource.schema=classpath:/sql/schema.sql
# spring.datasource.data=classpath:/sql/data.sql

# To keep mysql connection
spring.datasource.test-on-borrow=true
spring.datasource.validation-query=SELECT 1
# http.mappers.jsonPrettyPrint=true

#Application specific
authentication.oauth.clientid=rajithapp
authentication.oauth.secret=secret
authentication.oauth.tokenValidityInSeconds=1800

security.oauth2.client.clientId=rajithapp
security.oauth2.client.clientSecret=secret
security.oauth2.client.authorized-grant-types=authorization_code,refresh_token,password
security.oauth2.client.scope=openid

debug=true
